---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: maintainerrconfigs.media-operator.dev
spec:
  group: media-operator.dev
  names:
    kind: MaintainerrConfig
    listKind: MaintainerrConfigList
    plural: maintainerrconfigs
    singular: maintainerrconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MaintainerrConfig is the Schema for the maintainerrconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaintainerrConfigSpec defines the desired state of MaintainerrConfig.
            properties:
              connection:
                description: |-
                  connection defines how to connect to the Maintainerr instance.
                  Maintainerr uses API key authentication.
                properties:
                  apiKeySecretRef:
                    description: apiKeySecretRef references a Secret containing the
                      API key.
                    properties:
                      key:
                        description: key is the key within the Secret.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  tls:
                    description: |-
                      tls configures TLS settings for the connection.
                      Only needed when the app uses HTTPS with self-signed or private CA certificates.
                    properties:
                      caSecretRef:
                        description: |-
                          caSecretRef references a Secret containing a PEM-encoded CA certificate bundle
                          used to verify the target app's TLS certificate.
                          The Secret must contain the CA certificate under the specified key.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            type: string
                          name:
                            description: name is the name of the Secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      insecureSkipVerify:
                        description: |-
                          insecureSkipVerify disables TLS certificate verification.
                          WARNING: This makes the connection susceptible to machine-in-the-middle attacks.
                          Only use for testing or when the target app uses a self-signed certificate and
                          providing a CA is not practical.
                        type: boolean
                    type: object
                  url:
                    description: url is the base URL of the app (e.g., http://arr-sonarr.arr.svc.cluster.local:8989).
                    type: string
                required:
                - apiKeySecretRef
                - url
                type: object
              overseerrConnection:
                description: overseerrConnection configures the Overseerr/Jellyseerr
                  connection within Maintainerr.
                properties:
                  apiKeySecretRef:
                    description: apiKeySecretRef references a Secret containing the
                      app's API key.
                    properties:
                      key:
                        description: key is the key within the Secret.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  url:
                    description: url is the app URL (e.g., http://sonarr:8989).
                    type: string
                required:
                - apiKeySecretRef
                - url
                type: object
              plexConnection:
                description: plexConnection configures the Plex server connection
                  within Maintainerr.
                properties:
                  tokenSecretRef:
                    description: tokenSecretRef references a Secret containing the
                      Plex authentication token.
                    properties:
                      key:
                        description: key is the key within the Secret.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  url:
                    description: url is the Plex server URL (e.g., http://plex:32400).
                    type: string
                required:
                - tokenSecretRef
                - url
                type: object
              radarrConnection:
                description: radarrConnection configures the Radarr connection within
                  Maintainerr.
                properties:
                  apiKeySecretRef:
                    description: apiKeySecretRef references a Secret containing the
                      app's API key.
                    properties:
                      key:
                        description: key is the key within the Secret.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  url:
                    description: url is the app URL (e.g., http://sonarr:8989).
                    type: string
                required:
                - apiKeySecretRef
                - url
                type: object
              reconcile:
                description: reconcile configures reconciliation behavior (e.g., sync
                  interval).
                properties:
                  interval:
                    description: |-
                      interval overrides the default reconciliation interval (default: 5m).
                      Uses standard Kubernetes duration format (e.g., "30s", "5m", "1h").
                    type: string
                  prune:
                    description: |-
                      prune enables deletion of resources that exist in the app but are not declared in the CRD.
                      When true, for each resource type where at least one item is specified, any existing
                      resources not matching a declared item will be removed from the app.
                      Root folders and tags are never pruned.
                      Default: false (only create/update, never delete).
                    type: boolean
                type: object
              rules:
                description: rules defines media management rules for automatic collection
                  cleanup.
                items:
                  description: MaintainerrRule defines a media management rule.
                  properties:
                    action:
                      description: action defines what happens to matched media.
                      enum:
                      - delete
                      - unmonitor
                      type: string
                    conditions:
                      description: |-
                        conditions defines the criteria for matching media.
                        All conditions must be met for media to be matched (AND logic).
                      items:
                        description: MaintainerrRuleCondition defines a condition
                          for a media management rule.
                        properties:
                          field:
                            description: field is the media field to evaluate.
                            enum:
                            - addedAt
                            - lastViewedAt
                            - viewCount
                            - rating
                            - year
                            - fileSize
                            - resolution
                            - genre
                            - label
                            type: string
                          operator:
                            description: operator is the comparison operator.
                            enum:
                            - equals
                            - not_equals
                            - contains
                            - not_contains
                            - greater_than
                            - less_than
                            - before
                            - after
                            - in_last
                            - not_in_last
                            type: string
                          value:
                            description: |-
                              value is the value to compare against. For date fields, use duration format
                              (e.g., "30d" for 30 days, "6m" for 6 months). For numeric fields, use plain numbers.
                              For string fields, use the literal value.
                            type: string
                        required:
                        - field
                        - operator
                        - value
                        type: object
                      type: array
                    deleteFromDisk:
                      description: deleteFromDisk also deletes the media files from
                        disk (only when action=delete).
                      type: boolean
                    enable:
                      default: true
                      description: enable controls whether this rule is active.
                      type: boolean
                    libraryName:
                      description: libraryName is the Plex library this rule applies
                        to (e.g., "Movies", "TV Shows").
                      type: string
                    mediaType:
                      description: mediaType is the type of media this rule targets.
                      enum:
                      - movie
                      - show
                      type: string
                    name:
                      description: name is the display name of the rule.
                      type: string
                  required:
                  - action
                  - libraryName
                  - mediaType
                  - name
                  type: object
                type: array
              settings:
                description: settings configures general Maintainerr settings.
                properties:
                  collectionHandling:
                    description: collectionHandling controls how Maintainerr manages
                      Plex collections.
                    enum:
                    - keep
                    - delete
                    type: string
                  dryRun:
                    description: dryRun prevents actual deletions and only logs what
                      would happen.
                    type: boolean
                type: object
              sonarrConnection:
                description: sonarrConnection configures the Sonarr connection within
                  Maintainerr.
                properties:
                  apiKeySecretRef:
                    description: apiKeySecretRef references a Secret containing the
                      app's API key.
                    properties:
                      key:
                        description: key is the key within the Secret.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  url:
                    description: url is the app URL (e.g., http://sonarr:8989).
                    type: string
                required:
                - apiKeySecretRef
                - url
                type: object
            required:
            - connection
            type: object
          status:
            description: MaintainerrConfigStatus defines the observed state of MaintainerrConfig.
            properties:
              conditions:
                description: conditions represent the current state of the MaintainerrConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: lastSyncTime is the timestamp of the last successful
                  reconciliation.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
